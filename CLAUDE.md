@AGENTS.md

Read `docs/HISTORY.md` before starting work — it's a running log of
project decisions and changes. Add an entry there for any notable
decision you make.

If this project has a GitHub Project board, run `/sync-board` early in
the session (merges don't move board items to Done on their own).
